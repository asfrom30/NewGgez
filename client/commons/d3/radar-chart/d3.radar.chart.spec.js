'use strict';

describe('GGEZKR Radar Chart Test', function() {

  // Load the module that contains the `phoneDetail` component before each test
  beforeEach(module('ggez.radarChart'));

  // Test the controller
  describe('Radar Chart Controller', function() {
    it('should create Radar Chart', inject(function($componentController){
        var ctrl = $componentController('radarChart');

        
    }))
  });

});
